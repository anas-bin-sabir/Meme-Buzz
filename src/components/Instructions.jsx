export default function Instructions() {
  return (
    <p className="instructions">
      Click the button to change the image. You can change the top and bottom
      texts from the fields given below
    </p>
  );
}
